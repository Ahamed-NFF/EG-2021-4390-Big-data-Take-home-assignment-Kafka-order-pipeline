# Kafka runtime artefacts — the broker install and its log directory live
# outside the repo (see README), but keep these out just in case.
kafka_2.*/
kafka-data/
*.tgz
logs/
*.log
*.log.err

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Demo transcripts are regenerated by scripts/demo.ps1; the committed copy
# under docs/ is the one that is kept.
docs/demo-output-*.txt

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
